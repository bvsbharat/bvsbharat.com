---
export interface Props {
  noPadding?: boolean;
}

const { noPadding = false } = Astro.props;
---

<div class={`mx-auto ${noPadding ? "px-0" : "px-4"}`} style="max-width:1020px">
  <hr class="border-border/40 border-dotted" aria-hidden="true" />
</div>
