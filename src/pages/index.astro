---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Hr from "@/components/Hr.astro";
import Card from "@/components/Card.astro";
import Socials from "@/components/Socials.astro";
import { SITE } from "@/config";
import { SOCIALS } from "@/consts";
import { getCollection } from "astro:content";
import { getSortedPosts } from "@/utils/getSortedPosts";

const posts = await getCollection("blog");
const sortedPosts = getSortedPosts(posts, { excludeHackathons: false });
const featuredPosts = sortedPosts.filter((p) => p.data.featured);
const recentPosts = sortedPosts.filter((p) => !p.data.featured);

const featuredHackathons = getSortedPosts(posts, { excludeHackathons: false })
  .filter((p) => p.data.featured && p.data.tags.includes("hackathon"));
---

<Layout>
  <Header />
  <main id="main-content" class="flex-1">
    <section id="hero" class="pt-6 pb-4">
      <div class="flex flex-col sm:flex-row items-start gap-4">
        <a href="/about" class="block mx-auto sm:mx-0 group">
          <img
            src="/images/profile.png"
            alt={SITE.author}
            class="w-40 h-40 rounded-full object-cover flex-shrink-0 transition-all duration-300 group-hover:scale-105 group-hover:shadow-xl"
          />
        </a>
        <div class="flex-1 text-center sm:text-left">
          <div class="flex items-center gap-2 justify-center sm:justify-start">
            <h1 class="my-2 inline-block text-2xl font-bold sm:my-4 sm:text-3xl">
              Hi, I'm {SITE.author}.
            </h1>
            <a
              target="_blank"
              href="/rss.xml"
              class="inline-block text-accent"
              aria-label="RSS Feed"
              title="RSS Feed"
            >
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M4 11a9 9 0 0 1 9 9"/><path d="M4 4a16 16 0 0 1 16 16"/><circle cx="5" cy="19" r="1"/></svg>
              <span class="sr-only">RSS Feed</span>
            </a>
          </div>
          <p class="opacity-80">
            {SITE.desc}
          </p>
          {SOCIALS.filter((s) => s.active).length > 0 && (
            <div class="mt-4 flex flex-row items-center justify-center sm:justify-start">
              <Socials />
            </div>
          )}
        </div>
      </div>
    </section>

    <Hr />

    {featuredPosts.length > 0 && (
      <>
        <section id="featured" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">Featured</h2>
          <ul>
            {featuredPosts.slice(0, SITE.featuredPerIndex).map((post) => <Card post={post} />)}
          </ul>
        </section>
        {recentPosts.length > 0 && <Hr />}
      </>
    )}

    {recentPosts.length > 0 && (
      <section id="recent-posts" class="pt-12 pb-6">
        <h2 class="text-2xl font-semibold tracking-wide">Recent Posts</h2>
        <ul>
          {recentPosts.slice(0, SITE.postPerIndex).map((post) => <Card post={post} />)}
        </ul>
      </section>
    )}

    {sortedPosts.length > SITE.postPerIndex && (
      <div class="my-8 text-center">
        <a
          href="/posts"
          class="inline-flex items-center gap-1 text-accent decoration-dashed underline-offset-4 hover:underline font-medium"
        >
          All Posts
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
        </a>
      </div>
    )}

    {featuredHackathons.length > 0 && (
      <>
        <Hr />
        <section id="featured-hackathons" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">Featured Hackathons</h2>
          <ul>
            {featuredHackathons.map((post) => <Card post={post} />)}
          </ul>
        </section>
        <div class="my-8 text-center">
          <a
            href="/hackathons"
            class="inline-flex items-center gap-1 text-accent decoration-dashed underline-offset-4 hover:underline font-medium"
          >
            All Hackathons
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
          </a>
        </div>
      </>
    )}
  </main>
  <Footer />
</Layout>
